name: E2E Tests

env:
  XCODE_VERSION: 26.3

on:
  pull_request:
  release:
    types:
      - published

jobs:
  build-matrix:
    name: Build matrix with react-native versions
    runs-on: ubuntu-latest
    outputs:
      react-native-matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: |
          echo "matrix=['local', '0.73', '0.74', '0.75', '0.76', '0.77', '0.79', '0.81', '0.83', '0.85']" >> $GITHUB_OUTPUT

  build-android:
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        react-native: ${{ fromJSON(needs.build-matrix.outputs.react-native-matrix) }}
    runs-on: ubuntu-latest
    needs:
      build-matrix
    name: Build android for E2E tests for react-native ${{ matrix.react-native }}
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9

      - uses: actions/setup-node@v7
        with:
          cache: pnpm
          node-version: 22.x

      - uses: actions/setup-java@v5
        with:
          cache: gradle
          distribution: temurin
          java-version: 17

      - run: pnpm install --frozen-lockfile

      - run: pnpm install --frozen-lockfile
        working-directory: e2e-app

      - name: Setup react-native specific dependencies
        if: matrix.react-native != 'local'
        run: node scripts/compatibility.mjs
        working-directory: e2e-app
        env:
          REACT_NATIVE_VERSION: ${{ matrix.react-native }}

      - name: Install ${{github.event.release.tag_name}} version of the package
        run: pnpm add @fingerprintjs/fingerprintjs-pro-react-native@${{github.event.release.tag_name}}
        if: github.event_name == 'release'
        working-directory: e2e-app

      - name: Fix expo dependencies
        if: matrix.react-native != 'local'
        run: pnpm exec expo install --fix
        working-directory: e2e-app

      # `expo install --fix` aligns every bundled package to the Expo SDK, including coercing
      # react-native to the SDK's bundled version. Re-pin it to the version under test so the
      # matrix actually exercises that RN version rather than the SDK's default.
      - name: Pin react-native to the tested version
        if: matrix.react-native != 'local'
        run: pnpm add react-native@${{ matrix.react-native }}
        working-directory: e2e-app

      - name: Setup fresh node_modules
        if: matrix.react-native != 'local'
        run: rm -rf node_modules && pnpm install --frozen-lockfile
        working-directory: e2e-app

      - name: Prebuild
        run: pnpm prebuild
        working-directory: e2e-app

      - run: pnpm exec detox build --configuration android.emu.release
        working-directory: e2e-app

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: android-app-release-${{ matrix.react-native }}
          path: e2e-app/android/app/build
          retention-days: 3

  e2e-tests-android:
    needs:
      - build-android
      - build-matrix
    runs-on: ubuntu-latest
    name: Android E2E Tests for react-native ${{ matrix.react-native }}
    strategy:
      max-parallel: 3
      fail-fast: false
      matrix:
        react-native: ${{ fromJSON(needs.build-matrix.outputs.react-native-matrix) }}
    steps:
      - uses: actions/checkout@v7

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9

      - uses: actions/setup-node@v7
        with:
          cache: pnpm
          node-version: 22.x

      - uses: actions/setup-java@v5
        with:
          cache: gradle
          distribution: temurin
          java-version: 17

      - name: AVD cache
        uses: actions/cache@v6
        id: avd-cache
        with:
          path: |
            ~/.android/avd/*
            ~/.android/adb*
          key: avd-35

      - run: pnpm install --frozen-lockfile

      - run: pnpm install --frozen-lockfile
        if: github.event_name != 'release'
        working-directory: e2e-app

      - uses: actions/download-artifact@v8
        with:
          name: android-app-release-${{ matrix.react-native }}
          path: e2e-app/android/app/build

      - name: Get device name
        id: device
        run: node -e "console.log('AVD_NAME=' + require('./.detoxrc').devices.emulator.device.avdName)" >> $GITHUB_OUTPUT
        working-directory: e2e-app

      - name: Create AVD and generate snapshot for caching
        if: steps.avd-cache.outputs.cache-hit != 'true'
        uses: reactivecircus/android-emulator-runner@a421e43855164a8197daf9d8d40fe71c6996bb0d # v2.38.0
        with:
          api-level: 35
          target: 'default'
          arch: x86_64
          force-avd-creation: false
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          disable-animations: false
          script: echo "Generated AVD snapshot for caching."
          avd-name: ${{ steps.device.outputs.AVD_NAME }}

      - name: Create .env file
        working-directory: e2e-app
        run: |
          echo "MINIMUM_US_DEFAULT_PUBLIC_KEY=${{ secrets.MINIMUM_US_DEFAULT_PUBLIC_KEY }}" > .env
          echo "MINIMUM_US_DEFAULT_PRIVATE_KEY=${{ secrets.MINIMUM_US_DEFAULT_PRIVATE_KEY }}" >> .env
          echo "MINIMUM_US_SEALED_ENCRYPTION_KEY=${{ secrets.MINIMUM_US_SEALED_ENCRYPTION_KEY }}" >> .env
          echo "MINIMUM_US_SEALED_PUBLIC_KEY=${{ secrets.MINIMUM_US_SEALED_PUBLIC_KEY }}" >> .env
          echo "DEFAULT_EU_DEFAULT_PUBLIC_KEY=${{ secrets.DEFAULT_EU_DEFAULT_PUBLIC_KEY }}" >> .env
          echo "DEFAULT_EU_DEFAULT_PRIVATE_KEY=${{ secrets.DEFAULT_EU_DEFAULT_PRIVATE_KEY }}" >> .env

      - name: Setup react-native specific dependencies
        if: matrix.react-native != 'local'
        run: node scripts/compatibility.mjs
        working-directory: e2e-app
        env:
          REACT_NATIVE_VERSION: ${{ matrix.react-native }}

      - name: Detox test
        uses: reactivecircus/android-emulator-runner@a421e43855164a8197daf9d8d40fe71c6996bb0d # v2.38.0
        with:
          api-level: 35
          force-avd-creation: false
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          disable-animations: true
          arch: x86_64
          avd-name: ${{ steps.device.outputs.AVD_NAME }}
          script: cd e2e-app && pnpm exec detox test --configuration android.emu.release --headless --record-logs all --record-videos failing

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: android-detox-artifacts-${{matrix.react-native}}
          path: '**/detox-artifacts'
          retention-days: 3

      - uses: geekyeggo/delete-artifact@176a747ab7e287e3ff4787bf8a148716375ca118 # v6.0.0
        name: Delete build artifact
        if: success()
        with:
          name: android-app-release-${{ matrix.react-native }}

  build-ios:
    env:
      HOMEBREW_NO_AUTO_UPDATE: 1
      HOMEBREW_NO_INSTALL_CLEANUP: 1
    runs-on: macos-26
    name: Build iOS for E2E tests for react-native ${{ matrix.react-native }}
    strategy:
      max-parallel: 3
      fail-fast: false
      matrix:
        react-native: ${{ fromJSON(needs.build-matrix.outputs.react-native-matrix) }}
    needs: build-matrix
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9

      - uses: actions/setup-node@v7
        with:
          cache: pnpm
          node-version: 22.x

      - uses: maxim-lobanov/setup-xcode@ed7a3b1fda3918c0306d1b724322adc0b8cc0a90 #v1.7.0
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
          ruby-version: 3.2

      - run: pnpm install --frozen-lockfile
        if: github.event_name != 'release'

      - run: pnpm install --frozen-lockfile
        if: github.event_name != 'release'
        working-directory: e2e-app

      - name: Setup react-native specific dependencies
        if: matrix.react-native != 'local'
        run: node scripts/compatibility.mjs
        working-directory: e2e-app
        env:
          REACT_NATIVE_VERSION: ${{ matrix.react-native }}

      - name: Install ${{github.event.release.tag_name}} version of the package
        run: pnpm add @fingerprintjs/fingerprintjs-pro-react-native@${{github.event.release.tag_name}}
        if: github.event_name == 'release'
        working-directory: e2e-app

      - name: Fix expo dependencies
        if: matrix.react-native != 'local'
        run: pnpm exec expo install --fix
        working-directory: e2e-app

      # `expo install --fix` aligns every bundled package to the Expo SDK, including coercing
      # react-native to the SDK's bundled version. Re-pin it to the version under test so the
      # matrix actually exercises that RN version rather than the SDK's default.
      - name: Pin react-native to the tested version
        if: matrix.react-native != 'local'
        run: pnpm add react-native@${{ matrix.react-native }}
        working-directory: e2e-app

      - name: Setup fresh node_modules
        if: matrix.react-native != 'local'
        run: rm -rf node_modules && pnpm install --frozen-lockfile
        working-directory: e2e-app

      - name: Prebuild
        run: pnpm prebuild
        working-directory: e2e-app

      - run: pnpm exec detox build --configuration ios.sim.release
        working-directory: e2e-app

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ios-app-release-${{ matrix.react-native }}
          path: e2e-app/ios/build/Build
          retention-days: 3

  e2e-tests-ios:
    env:
      HOMEBREW_NO_AUTO_UPDATE: 1
      HOMEBREW_NO_INSTALL_CLEANUP: 1
    runs-on: macos-26
    name: iOS E2E Tests for react-native ${{ matrix.react-native }}
    needs:
      - build-ios
      - build-matrix
    strategy:
      max-parallel: 3
      fail-fast: false
      matrix:
        react-native: ${{ fromJSON(needs.build-matrix.outputs.react-native-matrix) }}
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9

      - uses: actions/setup-node@v7
        with:
          cache: pnpm
          node-version: 22.x

      - uses: maxim-lobanov/setup-xcode@ed7a3b1fda3918c0306d1b724322adc0b8cc0a90 #v1.7.0
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Install macOS deps
        run: |
          brew trust wix/brew
          brew tap wix/brew
          brew install applesimutils

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
          ruby-version: 3.2

      - uses: actions/download-artifact@v8
        with:
          name: ios-app-release-${{ matrix.react-native }}
          path: e2e-app/ios/build/Build

      - name: Start iPhone simulator
        uses: futureware-tech/simulator-action@e89aa8f93d3aec35083ff49d2854d07f7186f7f5 # v5
        with:
          model: 'iPhone 17'
          shutdown_after_job: 'false'

      - run: pnpm install --frozen-lockfile

      - name: Setup react-native specific dependencies
        if: matrix.react-native != 'local'
        run: node scripts/compatibility.mjs
        working-directory: e2e-app
        env:
          REACT_NATIVE_VERSION: ${{ matrix.react-native }}

      - run: pnpm install --frozen-lockfile
        if: github.event_name != 'release'
        working-directory: e2e-app

      - run: pnpm exec detox test --configuration ios.sim.release --cleanup --headless --record-logs all --record-videos failing
        working-directory: e2e-app
        env:
          MINIMUM_US_DEFAULT_PUBLIC_KEY: ${{ secrets.MINIMUM_US_DEFAULT_PUBLIC_KEY }}
          MINIMUM_US_DEFAULT_PRIVATE_KEY: ${{ secrets.MINIMUM_US_DEFAULT_PRIVATE_KEY }}
          DEFAULT_EU_DEFAULT_PUBLIC_KEY: ${{ secrets.DEFAULT_EU_DEFAULT_PUBLIC_KEY }}
          DEFAULT_EU_DEFAULT_PRIVATE_KEY: ${{ secrets.DEFAULT_EU_DEFAULT_PRIVATE_KEY }}
          MINIMUM_US_SEALED_ENCRYPTION_KEY: ${{ secrets.MINIMUM_US_SEALED_ENCRYPTION_KEY }}
          MINIMUM_US_SEALED_PUBLIC_KEY: ${{ secrets.MINIMUM_US_SEALED_PUBLIC_KEY }}

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ios-detox-artifacts-${{matrix.react-native}}
          path: '**/detox-artifacts'
          retention-days: 3

      - uses: geekyeggo/delete-artifact@176a747ab7e287e3ff4787bf8a148716375ca118 # v6.0.0
        name: Delete build artifact
        if: success()
        with:
          name: ios-app-release-${{ matrix.react-native }}

  e2e-tests-web:
    name: Web E2E tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9

      - uses: actions/setup-node@v7
        with:
          cache: pnpm
          node-version: 22.x

      - run: pnpm install --frozen-lockfile

      - run: pnpm install --frozen-lockfile
        if: github.event_name != 'release'
        working-directory: e2e-app

      - name: Install dependencies in e2e-app
        run: pnpm exec playwright install chromium
        working-directory: e2e-app

      - name: Install ${{github.event.release.tag_name}} version of the package
        run: pnpm add @fingerprintjs/fingerprintjs-pro-react-native@${{github.event.release.tag_name}}
        if: github.event_name == 'release'
        working-directory: e2e-app

      - name: Build web
        run: pnpm build:web
        working-directory: e2e-app

      - run: pnpm test:web
        working-directory: e2e-app
        env:
          MINIMUM_US_DEFAULT_PUBLIC_KEY: ${{ secrets.MINIMUM_US_DEFAULT_PUBLIC_KEY }}
          MINIMUM_US_DEFAULT_PRIVATE_KEY: ${{ secrets.MINIMUM_US_DEFAULT_PRIVATE_KEY }}
          DEFAULT_EU_DEFAULT_PUBLIC_KEY: ${{ secrets.DEFAULT_EU_DEFAULT_PUBLIC_KEY }}
          DEFAULT_EU_DEFAULT_PRIVATE_KEY: ${{ secrets.DEFAULT_EU_DEFAULT_PRIVATE_KEY }}
          MINIMUM_US_SEALED_ENCRYPTION_KEY: ${{ secrets.MINIMUM_US_SEALED_ENCRYPTION_KEY }}
          MINIMUM_US_SEALED_PUBLIC_KEY: ${{ secrets.MINIMUM_US_SEALED_PUBLIC_KEY }}
